v3.2.6
2026年08月29日
    - **来源 URL 的规范化补齐 RFC 3986 §6.2.3 的另外两项**：path 为空时补成 "/"、
      去掉默认端口（http 的 :80、https 的 :443）。此前只做了 scheme 与主机名小写，
      于是同一个站点在「来源 URL」Top N 里被拆成两条 —— 实测
      https://wave.com/ 9431 次、https://wave.com 3567 次。规范里这两个就是同一个 URI：
      「a URI that uses the generic syntax for authority with an empty path
      should be normalized to a path of "/"」。域名那一栏本来就是合并的，只有 URL 栏会分开。
      **只补空 path，绝不动别处的尾部斜杠**：https://wave.com/foo 和 https://wave.com/foo/
      是不同的 URL，服务器普遍当成不同资源（最常见的表现就是其中一个 301 跳到另一个，
      而「需要跳转」本身就证明它们不是同一个地址）。统一去掉或统一加上尾斜杠，
      会把真实存在的不同来源页面糊成一条
    - normalize-domains.php 的候选行预筛跟着一起改。它原来只查大小写，
      而 https://wave.com（path 为空）本来就是全小写的 —— 直接重跑回填会被预筛整个跳过，
      工具报「改动 0 行」而问题原封不动。这和 MySQL 排序规则那个坑是同一种形状：
      预筛与实际规则脱节，跑完一行没动还看不出问题。以后再加规范化规则，记得回来加判据
    - **修复迁移接口在「扫完但仍有失败行」时无法重试**。原来只在 pending > 0 时调 run()，
      而断点扫到头之后 pending 就是 0、失败行是单独记账的 —— 每轮 run() 开头的 retryFailures()
      正是补写这些行的唯一入口，于是接口一直回 blocked，哪怕数据库那边的障碍早就排除了。
      现在条件里带上「还有失败行」。响应新增 exhausted 与 failed_ids：
      都试满次数之后前端会明确告诉用户去跑 --retry-failed，而不是让他对着不动的进度条继续点
    - **修复降级标记写不进主库时后台提示在骗人**。markDegraded() 整个 try/catch 吞掉，
      而 isDegraded() 读失败按「未降级」处理 —— 队列照常在跑，后台却说「已自动降级为直写」，
      用户以为安全了而重复计数还在发生。markDegraded() 改为返回成败，ensure() 带出
      criticalRecorded，提示分成两种说法：写进去了才说「已降级」，没写进去就明说
      「队列可能仍在运行，请修复后重新保存设置，在此之前可以先关掉写入缓冲」。
      isDegraded() 读失败仍然按未降级处理 —— 反过来会让一次主库抖动关掉所有站点的队列
    - **修复从 config/current.yaml 启用时绕过队列排空**。保存设置那条路早就有 drainBeforeSwitch()，
      启用这条路一直没有：「禁用时队列没刷干净所以原样保留 → 改了 current.yaml 指向别的
      Redis / 统计库 → 启用」走下来，旧队列就留在原地没人认领。现在启用前也刷一次。
      这里不像保存设置那样直接拒绝 —— 启用被拒会让插件装不上，代价太大，改为尽力刷、
      刷不干净就把话放进启用提示里
    - 老键接管改用 RENAMENX。原来是先 exists(新键) 再 rename()，两步之间生产者完全可能
      刚好创建了新键、随后被 rename 整个覆盖，那一批消息就没了。RENAMENX 把判断和改名
      合成 Redis 内部的一步
    - **修复 config/current.yaml 里格式错误的行被静默跳过**。空行、注释、文档标记都滤掉之后
      还匹配不上的就是真写坏了，原来一个 continue 了事：那一项悄悄回退成默认值，
      其余项照常生效，而用户看到的是「配置已加载」—— 改了统计库地址却写错一个字符，
      表现就是统计悄悄写回了主库。未闭合的引号同理（`dbName: "x` 会把引号存进值里）。
      现在整份拒绝并报出行号；调用方收到异常会回退到 options 里已有的配置，
      比回退成表单默认值安全得多
    - normalizeUrl() 支持 IPv6 字面量主机名。原来的 [^/?#:]* 在第一个冒号处就停下，
      HTTPS://[FE80::ABCD]/x 只有 "[FE80" 被小写，后半段原样留着，等于没规范化
    - 直写统计表失败不再完全无声。异常仍然不往外抛（这条路跑在页面渲染里），
      但会写一条服务器错误日志 —— 缺 event_id 唯一索引时队列被强制降级，
      **直写是唯一的写入路径**，它一直失败而毫无痕迹的话，表现就是「统计突然不涨了」，无从查起
    - **修复失败行超过 500 条后被静默丢弃**。recordFailures() 原来在清单超过 MAX_FAILED_TRACKED
      时 array_slice 留下 id 最小的 500 个，而断点是照常推进的 —— 被截掉的那些 id 更大的失败行，
      既不在清单里、也已被断点越过，从此不会再被读到。更糟的是等清单里剩下的 500 条后来被修好清空，
      failures() 返回空，ensure() 就会打下完成标记，那批行被正式宣告「迁完了」。
      现在超限直接抛异常：run() 里的 saveCheckpoint() 随之不执行，这批（含失败行）下轮重读
    - **修复目标库已有自己的日志时，迁移会静默丢掉源数据**。COLUMNS 里包含 id，迁移是连主键
      一起复制的；旧版本走 Skipped 分支时目标库已经从 id=1 写过自己的访问日志，这些主键被占住之后，
      源表相同 id 的行撞上 ON CONFLICT DO NOTHING 被静默丢弃，而整批 INSERT 没报错，
      insertBatchDetailed 把它们计成写入成功，最后 done=true。实测：源表 50 行、目标库有 20 条
      自己的日志，迁完只剩 30 行，另外 20 行永久消失且被宣告「迁移完成」。
      现在首次迁移前先抽样检查迁移主键区间里有没有「不是迁移来的行」（foreignRowInRange()），
      发现就拒绝执行并说明要先清理，而不是迁一半。
      **不能改成让目标库自己分配 id**：3.2.0 之前的存量行没有 event_id，它们的重放幂等只靠这个主键
    - **修复没有断点时用目标表 MAX(id) 推导续传起点会跳过源数据**。reserveIdRange() 改为返回成败、
      不再吞异常；resumeFrom() 加两道闸门：抬高自增起点失败就不推导（从 0 重扫），
      推导出的候选断点还要用 looksMigrated() 比对源库同 id 行的 event_id（或 time+ip+path），
      确认它确实是迁移副本而不是目标库自己的日志。两道都不过就从 0 重扫 ——
      重扫只是慢，推错是永久漏掉源表前面一整截
    - **修复队列 fatal 路径不受 24 小时上限约束**。连接层失败（连不上、语句拼不出来）原来只
      markStuck() 就退出，从不比较 STUCK_SECONDS —— 只有逐行失败那条路会检查。
      于是持续性故障会让这批永远停在 processing，主队列照常堆积、涨到 MAX_LENGTH 之后
      从队首开始丢，丢的还是最早的数据。现在两条路走同一道闸门：卡满上限后整批转入死信
      （原因 db-environment / db-unknown），至少留下原始内容可查可回放
    - **失败行试满 3 次后补上恢复入口**。retryFailures() 永久排除达到 MAX_FAILED_ATTEMPTS 的行，
      对「一直是脏数据」是对的，但对「约束配错了、磁盘满了，修好之后」就成了死路：记录还在
      （完成标记打不下去），却再也不会被补写；而断点早已越过这些行，直接 --forget-failed
      等于永久漏掉它们。新增 Migrate::resetFailureAttempts() 与命令行 --retry-failed 把次数清零。
      --forget-failed 的提示也改成明说「等于永久放弃，除非 --from=0 整表重扫」
    - 修复两个回填工具在写入中途失败时，提示的续跑位置会跳过当前批次。
      $lastId 原来在行循环里就推进到批末，而分组 UPDATE 在其后 —— 本批可能只写了一部分，
      按提示 --from={批末} 续跑会把没写成的分组整个跳过。现在改为全部分组写成功之后才推进，
      失败时提示上一批的末尾（重跑已写成功的分组是幂等的，代价只是多读一批）
    - 两个自检脚本补上此前没覆盖的分支：selftest.php 增加连接层持续失败（fatal）与卡满放行；
      selftest-migrate.php 增加「目标库已有自己的日志」的脏目标库场景

v3.2.5
2026年08月29日
    - **来源域名统一小写后入库**。主机名不区分大小写（RFC 3986 §3.2.2），但入库时没做规范化，
      于是 wave.com 和 WAVE.COM 在 GROUP BY 里成了两个来源，同一个站点被拆开、Top N 的名次也失真。
      规范化落在 Queue::normalize()（入队和直写共用的收口）：
      referer_domain / entrypoint_domain 整列小写；referer / entrypoint 走新增的
      Queue::normalizeUrl()，**只把 scheme 和主机名小写，path 和 query 原样保留** ——
      后两者是区分大小写的，一起小写会把 /Article/Foo 变成另一个地址。
      用户信息（user:pass@）也保留原样，密码区分大小写。
      只规范化 *_domain 两列是不够的：控制台的「来源 URL」Top N 按 entrypoint 整串分组，
      HTTPS://WAVE.COM/x 和 https://wave.com/x 在那一栏照样会分成两条
    - **修复站内来源被当成外部来源**。getEntryPoint() 里判断「来路是不是本站」用的是
      逐字比较主机名，而主机名不区分大小写：来路写成 HTTPS://YOURSITE.COM/x 时跟 siteUrl
      对不上，于是自己站内的跳转被记成外部来源混进统计，还会被写进 cookie 一路带下去。
      现在先规范化再比，两边都按小写比对
    - 新增 tools/normalize-domains.php，把存量数据的这四列规范化。
      支持 --dry-run、--limit + --from 分批续跑，重复执行幂等。
      预筛条件**按引擎分开写**：MySQL 默认排序规则不区分大小写，`col <> LOWER(col)` 恒为假，
      照搬 PostgreSQL 的写法会让预筛把所有行都判成「不用改」，跑完一行没动还看不出问题。
      也正因为这个默认规则，本问题主要出现在 PostgreSQL 和 SQLite 上（两者默认比较区分大小写），
      MySQL 的 GROUP BY 本来就把大小写变体合在一起了

v3.2.4
2026年08月29日
    - **修复 UA 认不出来就当成人类**。isRobot() 原来只有两条判据（一条正则、一条名单），
      两条都不命中时 robot 记 0 —— 于是这样的记录被当成了真人访问：
        Hello from Palo Alto Networks, find out more about our scans in https://…
      实测同样被记成人类的还有 CensysInspect、InternetMeasurement、masscan，
      以及 **curl 和 wget**、空 UA、只有一个 "-" 的 UA。后两类影响面比单个扫描器大得多。
      现在判据按「最确定 → 最兜底」排成六条，任一命中即为机器人：
      ① 已知工具名（curl / wget / masscan / python-requests / okhttp / Java …）
      ② 收录的爬虫名单  ③ 名字里带 bot / spider / client / User 的正则
      ④ **UA 里写了 URL 或邮箱** —— 爬虫二十多年的惯例是把联系方式写进 UA（+http://…），
         真实浏览器没这个需要；名字取 URL 主机名或邮箱域名，同一家扫描器能聚到一起
      ⑤ UA 为空或只是占位符  ⑥ **兜底：浏览器和操作系统都认不出来**
      第 ⑥ 条是关键：真实浏览器的 UA 一定给得出浏览器或系统特征，两样都给不出的绝大多数是脚本。
      宁可把小众浏览器误记成机器人（看一眼 UA 就能发现），也好过让扫描器混在人类流量里
    - 名单改为**按词边界匹配**，并清掉三类坏条目。原来是「去空格小写后求子串」，没有边界，实测：
      * `Custo` 命中了长文本 UA 里的 **customers**（Palo Alto Expanse 那条），robot_id 记成毫无意义的 Custo
      * `Presto` 命中 **Opera 12** 的 Presto/2.10.289 —— 真实浏览器被判成机器人
      * `Ask` 会命中任何含 ask 的串（task、Basketball…）
      同时删掉了永远匹配不到真实 UA 的条目：`Alexa (IA Archiver)`、`Java (Often spam bot)`
      括号里是给人看的注释却被当成匹配串；`WGet tools` 归一化成 wgettools，对不上 `Wget/1.21.3`。
      `Presto`、`TencentTraveler`（腾讯 TT）是真实浏览器，已移除，别再加回来
    - 名单的匹配挪到通用正则**前面**：正则抓的是「紧挨着 bot/spider 的那一个词」，
      名字常截到一半 —— `Sogou web spider/4.0` 会被记成 "web spider"
    - 新增 tools/reclassify-robots.php，按新规则重算存量数据的 robot / robot_id / robot_version。
      只改这三列；支持 --dry-run 预览改动明细、--limit + --from 分批续跑；
      改动按目标值分组用 WHERE id IN (...) 打包，两百多万行不会退化成几十万次单行 UPDATE。
      重复执行是幂等的

v3.2.3
2026年08月28日
    - **修复大表推迟迁移时，目标库的新日志会吃掉源表数据**。抬高目标表自增起点这一步
      原来只在 run() 里做，而数据量超过 AUTO_LIMIT 时 ensure() 走的是 Skipped 分支、
      根本到不了 run()。此时配置已保存，前台立刻开始往目标库写新日志、id 从 1 起，
      正落在迁移区间里：旧的断点推导（目标表 MAX(id)）直接跳到这些新日志的最大 id，
      源表前面一整截被静默跳过，migratedCount() 还把它们算成「已迁移」，连 pending 都是错的。
      现在 reserveIdRange() 排在 Skipped 判断之前，目标库新产生的日志一律落在源表最大 id 之上
    - **迁移断点改为自己存**（主库 options 的 access_migrate_checkpoint，按目标库指纹分组），
      不再从目标表 MAX(id) 推导。除上一条外，旧推导还有第二个错：写失败的行在目标表里是空洞，
      MAX(id) 天然跳过它们，于是失败行永远不会被重读。没有断点时（老版本升上来）
      仍退回旧推导一次，之后就存下来
    - **失败行现在真的会被重试**。每轮迁移开工前先补写一次（retryFailures()），
      失败记录从「id 列表」变成「id => 已尝试次数」，试满 MAX_FAILED_ATTEMPTS(3) 次后
      不再自动补写但记录保留，完成标记照样打不下去。源行已被删掉的从清单里摘掉，
      否则它永远补不回来、也就永远挡着完成标记。断点照常推进到批尾，
      失败行由清单单独负责 —— 让断点停在失败行之前会把整个迁移永久卡死在同一个位置
    - Migrate::writeFailed() 不再吞异常。这一行是唯一挡住完成标记的东西：
      写失败却当成写成功，下一轮 failures() 读到空就 mark() 下去，那几行数据谁也不会再想起来
    - **修复源库查询失败被当成「没有历史数据」并标记完成**。tableExists / sourceCount /
      sourceMaxId 都把「查询失败」和「确实为空」答成同一个值（false / 0），而 ensure() 里
      每个这样的分支出口都是 mark()，run() 里则是 done=true —— 主库连接抖一下，
      整张源表就被永久判定为没有历史数据。新增 tableExistsStrict / sourceCountStrict /
      sourceMaxIdStrict，迁移路径一律用它们；容错版保留给展示类调用
    - 进度改按断点算（pendingCount：源表里 id > 断点的行数），不再按目标表行数算 ——
      目标库自己产生的新日志不是「已迁移的历史数据」。命令行脚本里
      「pending 为 0 就清掉失败记录并标记完成」也一并改掉：换成断点口径之后，
      pending 为 0 与「有失败行」完全可以同时成立，照旧做就是把没迁过去的数据一笔勾销。
      现在先真的补写一次，补不上就拒绝标记并以 2 退出
    - **修复缺 event_id 唯一索引时队列照常运行**。这个索引是「至少一次投递」变成
      「恰好一次结果」的唯一依靠，它建不起来（无建索引权限、存量数据已有重复值）时，
      原来只在后台留一句黄字，插件照常启用、队列照常跑 —— 进程被杀后 processing 重放、
      或命令行重跑一次，同一条访问日志就被重复计入统计，且毫无迹象。
      现在 Schema::ensure() 实地复核并返回 critical，同时把结论写进 options
      （access_schema_degraded，按指纹分组）；Core 写入前检查，缺索引就强制走数据库直写。
      修好之后标记自动清掉、恢复走队列。直写慢是看得见的，统计悄悄变错不是
    - **修复保存设置时先提交配置、后建表，失败会留下半切换状态**。DDL 权限不足、
      库名写错时，配置已经指向新库而那个库里连表都没有，此后每次写入都在往不存在的表里插，
      而 writeLogs() 的异常是被吞掉的 —— 页面一切正常，统计无声停摆。
      现在先用新配置建表并跑完 Schema::ensure()，成功了才提交配置，失败则原配置继续生效
    - **修复 config/current.yaml 解析失败会把已有配置覆盖成表单默认值**。
      启用插件时 Typecho 会再调一次 configHandle($isInit=true)，传进来的是控件默认值；
      原来文件读不出来就直接用它，于是一个 YAML 语法错误就把后台填好的 Redis 地址、
      独立统计库、IPinfo token 全抹掉，而启用提示写的却是「本次沿用已有配置」。
      现在回退顺序是：配置文件 → options 里已有的配置 → 表单默认值
    - **接口补上 CSRF 防护**。原来只有 isAdmin()，那只回答「你是不是管理员」，
      回答不了「这次请求是不是你自己发起的」；migrate 还是个 GET 写操作，
      管理员在登录状态下访问任意第三方页面，一个 <img src=".../access/migrate.json"> 就能触发。
      现在写接口（deleteLogs / migrate）强制 POST 否则 405，五个接口一律校验 token
      （hash_equals 定时比较）。token 用 Typecho 的 Widget\Security 生成，含站点密钥、
      用户 authCode 与 uid，但**绑固定作用域而不是 referer**：控制台用 history.pushState 切 Tab，
      绑 referer 的话切一次就全失效，而 Referrer-Policy 剥掉 referer 时接口会对那些用户直接全废
    - 新增 tools/selftest.php 与 tools/selftest-migrate.php，可在真实 PostgreSQL + Redis 上
      验证上述两类故障路径。两者都强制隔离（拒绝生产前缀 / 拒绝 redis-db 0 /
      在一次性库里自建源库与目标库），结束自动清理
    - **修复写库故障被误判成脏数据后整批倒进死信**。判定原来靠 Migrate::alive()：
      一条都没写进去时探一次活，活着就认定「这批是脏数据」全部转死信。可 alive() 走的是
      SELECT —— 实测 PostgreSQL 16 下，库处于只读事务（备库 / default_transaction_read_only）、
      或账号 INSERT 权限被回收时，SELECT 1 照常成功而每一条 INSERT 都失败；磁盘写满同理。
      于是一次运维故障会把整个队列倒进死信，死信满 DEAD_MAX_LENGTH 之后从最旧的开始丢，
      静默的大规模数据丢失。现在按 SQLSTATE 分类（Database::classifyWriteError() +
      新增的 WriteErrorKind）：只有 22（数据异常）和 23（完整性约束）两类算「这一行的错」
      才转死信；08/25/40/42/53/54/55/57/58/XX/HY 一律算环境问题留着重试；认不出来的按环境处理。
      42 类（表不存在、列不存在、权限不足）特意归到环境而不是数据 —— 它对每一行都一样，
      归成数据的话一次结构没升好就会把整个队列清空
    - insertBatchDetailed() 不再把异常整个丢掉，返回值新增 kinds（失败行下标 => 归类）
      和 fatal（语句还没发出去就整批失败时的归类）。以前调用方只拿得到「哪几行失败」，
      分不出为什么失败，才不得不去猜
    - 新增 STUCK_SECONDS（24 小时）给「留着重试」封顶。全部留着重试是对的，但一条谁也认不出的
      错误可以永远占着队首，后面的消息堵到队列涨满、从最旧的开始丢 —— 那还是丢数据，只是换了位置。
      队首批次卡满 24 小时后放行，写不进去的按各自归类转入死信（db-environment / db-unknown），
      与真正的脏数据（db-rejected）在死信里可区分。取一整天是为了盖过真实故障的修复时间
    - Migrate::alive() 改用 Db::WRITE 探活，并在注释里写明它只能回答「连接通不通」，
      回答不了「写得进去吗」，不能再拿它做脏数据判定
    - **修复 processing 读取失败被当成「队列为空」**。leftover() 里 lRange 返回 false 时
      原来直接当空数组，流程转去 claim() 取新一批 RPUSH 到 processing 尾部，
      而确认那一步是按**位置** LTRIM 前 count 个 —— 砍掉的正是前面那批还没落库的消息，
      留下的反而是已经落库的。砍错批次直接丢数据。现在照 claim() 的写法抛异常停手
    - LTRIM 确认失败不再被当成成功：当成成功的话这批下轮会被 leftover() 再捡一次，
      而 attempted/written 已按成功计过数，调用方会读到一个假的成功
    - **修复前端埋点 URL 未编码**。原来是 '?u='+location.pathname+location.search+location.hash，
      页面地址带 # 时拼出来的 # 是真 fragment，后面的 &cid&mid&rand 浏览器根本不发送，
      整个埋点失效；带 & 时后续参数被拆散，页面地址里只要有 ?cid=xxx 就会顶掉真实的 cid，
      统计挂到错误的文章上。改为 encodeURIComponent(...) 整体编码
    - **修复 tools/migrate.php 的 --dry-run 并非只读**。它原来排在「pending === 0 就标记完成」
      之后，于是一个名字叫「只看不动」的开关做了整个脚本里最不可逆的那件事：写下
      access_migrate_done。这个标记一旦写下，isMarked() 此后每次直接返回，源表再也没人看一眼。
      现在 dry-run 挡在所有写入分支之前，--forget-failed 的清理也一并纳入
    - Redis 键名前缀由 typecho_access: 改为 plugin:access:，与本站其他插件
      （Accelerate 的 plugin:accelerate:）统一到 plugin:{插件名}: 这套命名上。
      指纹段不动，完整键名仍是 plugin:access:{12 位指纹}:...。
      **换前缀不做接管**：旧前缀下带指纹的那一代键（typecho_access:{指纹}:queue 等）
      不会被改名搬过来，升级前请先把队列刷干净 —— 跑一次 tools/flush-queue.php，
      或者在后台禁用一次插件 —— 否则那批没落库的访问日志会滞留在旧键上无人消费。
      更早的、加指纹之前的第一代老键（typecho_access:queue）仍照旧自动接管
    - 全表聚合的「正在重算」标记拆成互斥锁 + 失败退避两个键。以前是一个键兼任两职：
      算失败就不删标记、让它自然过期当退避。可这两件事要的存活时间方向相反 ——
      互斥锁要长到覆盖一次重算的最坏耗时，退避只要挡住前端接下来几轮轮询 ——
      合用一个键时只能二选一，选长了失败后要等很久才肯重试，选短了锁形同虚设。
      现在 {键}:computing 走 AGGREGATE_LOCK_TTL，{键}:backoff 走 AGGREGATE_BACKOFF_TTL(300s)
    - AGGREGATE_LOCK_TTL 由 300 秒提到 1800 秒。**这个锁没法靠续租兜底**：
      续租要求持锁进程周期性醒过来，而重算是一次阻塞的 PDO 调用，PHP 停在驱动的 read 上，
      中间没有任何执行点。队列的 LOCK_TTL 敢取 30 秒是因为刷库天然按批切开、批间可以续租，
      这里没有那个缝。取值按实测最坏情况留余量：n_distinct 没调之前单条「总计独立 IP」
      跑过 643 秒，而原来的锁 300 秒就过期了 —— 下一个请求随即再起一条同样的全表聚合，
      恰好是这把锁本来要防的形状。两个方向代价不对等，所以宁可给长：给长了只是崩溃后
      重算被推迟，陈旧值照常返回（AGGREGATE_STALE_TTL 有 7 天），用户看到的只是稍旧的数字
    - 重算锁的锁值改为一次性随机 token，释放走 Lua 比对后删除。原来锁值固定是 '1'，
      分不出「我的锁」和「别人的锁」：重算跑过 TTL 之后锁已被下一个请求抢走，
      收尾时那个 DEL 删的是新持有者的锁，于是第三个请求又能抢到，同一个聚合上并排跑好几条。
      刷库锁当年踩过同一个坑（锁值是 PID），别在这儿再踩一遍。
      超时的重算仍然会把算出来的值写回缓存 —— 它总比缓存里那个新，不写才是浪费
    - 埋点限流的 INCR 与 EXPIRE 合成一段 Lua。原来分两次往返，而 EXPIRE 只在 hits===1
      时发一次：连接抖一下、或恰好在两条命令之间断开，就留下一个**永不过期**的计数键，
      且再没有第二次机会补 TTL。单个键很小，但泄漏按「分钟 × IP」累积。
      顺带少一次往返 —— 这段代码每一次匿名埋点请求都要跑。
      另外脚本执行失败时 eval 返回 false、转 int 得 0，那不是「一次都没来过」而是「没数着」，
      现在显式判掉并放行，不再让限流悄悄失效
    - 卸载清理改为新旧两个前缀各扫一遍。以前 SCAN 只用 Cache::BASE 拼模式，
      换前缀之后就只扫得到新键，旧前缀下的东西会永远留在 Redis 里没人认领
    - Cache::isLegacyKey() 的判定跟着重写。旧写法是「以 BASE 开头且第一段不是
      12 位十六进制」，换前缀后这句话对新前缀恒假、等于失效。现在改判旧前缀，
      并且**带指纹的那一代必须逐字比对本站点指纹** —— 只要「以 typecho_access: 开头」
      就认下来的话，同一个 Redis 上别的站点的 typecho_access:{别人的指纹}:queue
      会被当成自己的老键删掉，正好把当初加指纹要修的毛病原样放回来
    - Queue::isLegacyDataKey() 同步覆盖旧前缀下的两代队列键。它的作用是在
      「保留队列」的卸载清理里拦住别删，漏认一代就等于把没落库的数据当缓存删掉
    - Health::cacheKey()（熔断标记的 **APCu** 键，不是 Redis 键）不再自己拼
      'typecho_access:' . 指纹 . ':'，改走 Cache::key()。它跟 Redis 键共用一套命名，
      却在两个类里各拼了一遍，改前缀时漏掉一处是迟早的事。
      APCu 那边的标记 30 秒就过期，换键名没有迁移问题

v3.2.2
2026年08月28日
    - 来源统计自动滤掉不是来源地址的值。Referer 头完全由对方控制，扫描器每天往里塞
      探测串（(select(0)from(select(sleep(15)))v)、if(now()=sysdate(),sleep(15),0) 之类），
      它们被当成一次「来源」记下来，然后占着来源 Top 20 的前排 —— 那些串往往被刷出很高的次数。
      判定用白名单而不是黑名单：黑名单在这件事上必然失败，payload 的花样无穷，
      而且拿 sleep/select/union 去匹配迟早误伤查询串里带这些词的正常地址。
      真正该问的是「它是不是一个来源地址」——带 scheme 和主机名、中间没有空白的绝对 http(s) URL。
      写入侧 getEntryPoint() 先过一遍（顺带挡住 payload 被写进 cookie、被后续每次请求带回来），
      读取侧在 SQL 里再挡一次（Top N 是数据库排序取前几名，取回 PHP 再筛会筛得不足 N 条）
    - PostgreSQL 的 ip 列固定 n_distinct 估计（建表脚本 + 结构升级步骤）。
      ANALYZE 从三万行样本外推 n_distinct，对高基数列出了名地不准：实测 314 万行的表
      真实有 45 万个不同 ip（14.3%），却被估成 22,878（0.73%），低估 20 倍。
      后果不是差一点而是选错计划 —— 规划器按两万多组规划哈希表，实际要装四十几万组，
      超出 work_mem 后 HashAggregate 落盘重分区，概览的「总计独立 IP」跑了 643 秒，
      控制台稳定 504。估对之后改走「按 (ip, ua) 索引顺序相邻去重」，不建哈希表，
      耗时降到 1 秒。这个设置重建表就会丢失且毫无征兆，所以一并纳入每次启用的实地校验

v3.2.1
2026年08月26日
    - 修复控制台打开一次就把最贵的缓存删一次。概览按 today→yesterday→total→referer→
      pie→month 逐段请求，第一段 today 会同步刷队列，而刷完的缓存失效名单里
      无条件包含 overview:total、referer:*、post_pie:* —— 恰好是后面三段要用的
      四个全表聚合。等于每打开一次控制台，它们都要从头算一遍，缓存形同虚设。
      实测 DISTINCT ip 单次 228 秒、占整个数据库 75% 的时间，控制台稳定 504。
      现在这几项不再由写入方删除，改为各自带新鲜期，按日期的键仍照常失效
      （那是当初为了修跨零点数字不更新才加的，不能动）
    - 全表聚合改为「陈旧优先 + 后台重算」：缓存过期时先把旧值返回，重算挂到
      fastcgi_finish_request 之后做，读的人一秒都不用等。冷缓存时不当场算，
      而是回 done=false 让前端按 retry_after 稍后再问 —— 一条没有上界的查询
      跑起来可能比 nginx 的 fastcgi_read_timeout 还久，那时浏览器早已 504，
      PHP 却还在磨，刷新几次就把 FPM 的 worker 堆满
    - 重算加了互斥标记：冷缓存时前端会反复轮询，不挡的话每问一次就多一条
      全表聚合在跑。算失败时标记不主动清除，靠自然过期充当失败退避
    - 修复控制台的双重 HTML 转义。前端渲染时每个字段都已经用 createTextNode
      转义过，服务端 htmlEncode() 再转一遍，页面上就会显示出 &#039; &quot;
      这类字面量而不是引号本身 —— 扫描器往 Referer 头塞注入探测串时尤其明显。
      涉及日志列表和来源 Top N 两处。缓存里已有的旧值不需要手动处理：
      新格式带信封（at + data），读到旧的裸数组会当成未命中重算一次
    - 日志页的来源不再无条件渲染成链接。referer 完全由对方控制，
      escapeHtml 只防属性闭合逃逸，拦不住 href="javascript:..." 这类伪协议。
      现在只有 http:// 和 https:// 才做成链接（判断前先剔除控制字符），
      其余按纯文本显示；链接补上 rel="noopener noreferrer nofollow"
    - 修复一部分 IPv6 地址被静默存错。ip 列存的不是地址文本而是地址的十进制整数表示，
      上限应为 2^128-1 的位数即 39 位，MySQL/SQLite 建表脚本和队列归一化以前却按 38 处理。
      十进制数截掉末位等于除以 10，于是 fc00::/7、fe80::/10、ff00::/8 这些恰好 39 位的地址
      会被存成另一个看起来合法的地址；2000::/3 全球单播是 38 位，所以这个问题长期没被发现。
      结构升级现在会自动把 MySQL 存量表的 ip 列扩为 char(39)——只在探测到列宽不够时才执行，
      因为 MySQL 改 CHAR 长度会重建整表并阻塞写入。升级完成前请尽快重新启用一次插件：
      在「文件已换、设置还没重新保存」的窗口内，这类地址会被 MySQL 严格模式拒绝而转入死信队列
    - content_id/meta_id 只接受整数或纯十进制数字字符串，不再把 1e3、带首尾空格等输入
      静默转换成另一个合法 ID
    - 修复死信队列写入失败后仍然确认队列的问题。pushDead() 以前无视 rPush 的返回值，
      调用方拿到条数就无条件裁掉 processing，于是「没进死信、也没进数据库」的消息被无声删除。
      现在写入失败直接抛异常，整批留在 processing 等下次重试
    - 死信消息编码不再静默丢条目。payload 是访客可控数据，可能不是合法 UTF-8，
      json_encode 会返回 false，而这条消息随后照样被裁掉。现在先做非法字节替换，
      再退到 base64 保留原始字节，两者都失败才拒绝确认
    - 取批次的 Lua 脚本改为「先写 processing，后裁主队列」。Lua 只保证不被别的命令打断，
      不保证出错回滚：先裁后写时一旦 RPUSH 失败，消息就同时不在主队列也不在 processing。
      反过来最坏只是留下副本，由 event_id 的唯一索引挡掉重复
    - 取批次失败不再伪装成「队列已空」。eval 出错时 phpredis 返回 false，
      以前被转成空数组，flush() 于是判定正常收工、退出码 0，一条丢数据的路径连告警都没有
    - 确认 processing 之前重新校验刷库锁。整批 INSERT 失败会退化成上千次逐行写，
      慢库上足以超过锁的 30 秒存活期；锁一过期，另一个消费者读到同一个 processing，
      两边各自按位置 LTRIM，后动手的那次砍掉的就是对方刚取走、还没落库的消息
    - 表结构升级不再「建索引失败却记成功」。event_id 唯一索引的创建异常以前被无条件吞掉，
      版本号照记 3.2.0，此后每次启用都直接返回，幂等保护实际从未生效。
      现在只容忍「索引已存在」，其余错误阻止记录版本号；并且每次启用都实地校验关键列与索引，
      缺了就地补齐，补不上就把版本号退回去。新增 Database::uniqueIndexOn() 按列而不是按索引名判断
    - 队列还有积压时，改动 Redis 地址、写入缓冲开关或统计数据库将被拒绝保存。
      以前只给一句提示、配置照样保存。提示文案也分了场景：Redis 变了是够不着，
      只换库是会被写进错误的库——原来那句「新配置不会再处理」在后一种情况下是反的。
      新增「队列归属变更」设置项，选「强制切换一次」可放行，保存后自动复位
    - 历史迁移不再永久跳过失败行。断点以前无条件推进到批次末尾，失败行落在断点之前从此没人再看，
      扫到头还返回 done=true。现在失败行先原地重试一次，仍失败就停止扫描并把源行 id 记进 options，
      有记录在案就绝不写迁移完成标记；整批写入失败（连不上目标库）同样不推进断点。
      控制台和命令行都会明确报出来，不再卡在同一个百分比上无限轮询。
      命令行新增 --forget-failed 用于确认放弃这些行
    - Redis 键名加入站点指纹（typecho_access:{12 位指纹}:...），指纹取自插件目录路径。
      多个 Typecho 共用一个 Redis DB 时不再串队列、串锁、串缓存。
      升级后第一次保存设置或跑一次 flush-queue.php 会自动接管老键上的遗留队列；
      卸载清理也只删本站点的键和升级前的老键，不再动同一个 Redis 上别的站点
    - flush-queue.php：写入队列被禁用后仍会把存量刷完（关开关只影响新消息入队）；
      队列已空但死信非空时以 1 退出而不是 0；刷完后读不到队列长度不再显示成「剩余 0 条」，
      而是如实报未知并以 1 退出
    - 启用插件时补充检查 mbstring、GMP 扩展与 64 位 PHP。
      Queue 按字符截断列宽用 mb_*，IPv6 换算用 gmp_*，event_id 的时间戳左移依赖 64 位整数
    - README 补充 Redis 运维边界：必须开启 AOF 持久化、不能配会淘汰队列键的 eviction 策略、
      主从切换与 appendfsync 的数据丢失窗口，以及「仅支持 standalone，不支持 Redis Cluster」
      （取批次的 Lua 同时操作两个无 hash tag 的键，Cluster 上会报 CROSSSLOT）

v3.2.0
2026年08月26日
    - 新增数据表结构版本号（协议版本）：记录在 Typecho 主库的 options 里，按统计库分别登记。
      启用插件、或在后台保存设置时，若发现版本与代码不一致就自动升级现有表结构。
      这个版本号只在表结构真正变化时才跟着插件版本走，其余发布保持不动，
      免得绝大多数版本都白跑一遍升级检查
    - 数据表新增 event_id 列与唯一索引，写入具备幂等性。每条访问日志在入队时生成一个
      32 位标识并随消息一起保存，重试时沿用同一个值。队列做的是「至少一次」投递——
      写库成功但从 processing 确认之前进程被杀，下一轮会把同一批再写一遍——
      有了唯一索引，重复的那次会被数据库挡下来，结果等同于「恰好一次」
    - 写入改为「唯一冲突即跳过」：MySQL 用 ON DUPLICATE KEY UPDATE，
      PostgreSQL 用 ON CONFLICT DO NOTHING，SQLite 用 INSERT OR IGNORE。
      MySQL 特意不用 INSERT IGNORE：它连字段超长这类数据错误也一并吞掉，
      而死信队列正是靠「数据库明确拒绝」来把脏数据挑出来的
    - 存量数据的 event_id 保持 NULL。三种数据库的唯一索引都允许多个 NULL，老数据不受影响，
      只是那部分不具备幂等能力
    - 升级插件文件后若还没重新启用，表结构仍停在旧版本，此时写入会自动摘掉 event_id 继续进行，
      不会因为缺列而整批失败——升级动作本身不应该把统计写崩
    - 禁用插件并勾选「数据清理」时，一并清除记录在案的表结构版本

v3.1.5
2026年08月26日
    - 写入队列改为「主队列 -> processing -> 死信」三段式，消费者用一段 Lua 原子地取走一批，
      不再「先读、写库、再按位置裁剪」。原来生产者在队列超过上限时的裁剪一旦插在读与裁之间，
      消费者就会裁掉刚写进来、还没落库的消息
    - 消费者取走消息后崩溃不再丢数据：那一批停在 processing 里，下次刷库先把它捡回来
    - 刷库锁改用随机 token，释放时用 Lua 比对归属，并在刷库期间续租；抢锁从页面渲染前
      推迟到 shutdown 真正开始刷库时。原来锁值是进程 PID、释放时直接 DEL，
      超时的旧消费者收尾时会删掉新消费者的锁，同一个队列上于是出现多个消费者
    - 锁存活时间由 60 秒调整为 30 秒：有续租之后不需要覆盖整次刷库，进程被杀后释放更快
    - 单次刷库的上限改为按「取出条数」计算，并新增 20 秒墙钟上限。原来按「写入成功数」计数，
      一批消息大量写失败时循环不推进，队列里若有大量无效 JSON 会一次吃掉整个队列
    - 新增死信队列 typecho_access:queue:dead：解析不了、或数据库明确拒绝的消息移入其中并保留
      原始内容，不再无声丢弃。原来一批 1000 条只要有 1 条写入成功，其余 999 条就会被一并删掉。
      死信上限 1 万条，超出后丢弃最旧的
    - 整批写入失败时先探活数据库，区分「数据库不可用」（保留重试）与「整批都是脏数据」（转入死信）。
      原来一律按前者处理，队尾只剩一条写不进去的记录时队列会永久卡死
    - 队列落库后按受影响的日期精确失效统计缓存（总计、来源、文章饼图，以及对应的日与月）。
      原来只更新时间戳，跨零点之后才把前一天的队列写进数据库的话，前一天的统计会带着错误数字
      挂到长缓存自然过期（最长 40 天）为止；删除日志时同样会失效对应缓存
    - 统一 Redis 连接的创建，新增读写超时（前台 3 秒，命令行 5 秒）。原来只设了连接超时，
      TCP 连上但服务端不回话时会一直等到 PHP 的默认套接字超时；实测一个握手后不回话的地址
      耗时 60.06 秒，设了读超时之后按超时值返回
    - 访问日志各字段按建表脚本的列宽截断（UA / URL / Referer / 入口点等），
      cid、mid 收进 int unsigned 范围。原来不设上限，一条超长记录会让批量 INSERT 失败并
      退化成上千次逐行 INSERT，而这个入口是匿名可达的
    - 前端统计模式的 GIF 接口增加频率限制（每 IP 每分钟 60 次，依赖 Redis）
    - 修复前端统计模式的条件写反：writeType=0（前端）时 GIF 接口不记录日志，
      writeType=1（后端）时该接口仍可被匿名调用重复写入
    - 停用插件时改为循环刷库直到取空，并按结果决定是否清理 Redis：还有未落库数据且未勾选
      「数据清理」时，队列与死信原样保留，下次启用继续写入，不再随缓存一起被删除；
      删表失败也不再让插件卡在「停用不掉」的状态
    - 在后台保存设置时，若改动会切换队列归属（Redis 地址或密码、关闭 Redis、关闭写入队列、
      更换统计数据库或表前缀），先用旧配置把积压刷完再保存
    - tools/flush-queue.php：数据库不可用、刷库中断、锁易主，或有消息转入死信时以退出码 1 结束
      （--lenient 可豁免最后一项），错误信息改走 STDERR；新增 --deadline 参数；
      不再把 Redis 故障显示成「队列为空」后以成功退出

v3.1.3
2026年08月25日
    - 启用插件（以及在后台保存设置）时会探测一次 Redis 可用性：Redis 只是加速器，
      连不上不阻断启用，缓存与写入队列自动降级，统计功能完整，只在提示里说明原因；
      恢复后无需改动，插件会自动重新连接
    - Redis 连接超时由 3 秒收紧到 0.5 秒，并新增熔断：连接失败后 30 秒内直接走降级路径，
      不再尝试连接。Core 是在 Widget\Archive::beforeRender 里构造的，不可达的地址
      （防火墙丢包、DNS 黑洞、容器网络不通）会等满整个超时，没有熔断的话每个前台请求
      都要白等一次；实测不响应的地址：未熔断 502 ms，熔断后 0 ms
    - 启用时的探测会顺带把结果写进熔断器，因此第一个访客也不必先撞一次超时
    - 熔断状态优先存于 APCu（共享内存，自带 TTL），没有该扩展时退回临时目录下的标记文件
      （用 mtime 当时间戳）。APCu 为可选依赖，缺失不影响任何功能
    - 标记文件放在临时目录下权限 0700 的专属子目录，文件名带插件目录指纹，同机多站互不干扰

v3.1.2
2026年08月25日
    - 新增配置文件支持：启用插件时若存在 config/current.yaml 则按它设置，
      禁用插件时把当时的配置写回同一个文件，便于随代码部署或迁移站点后恢复
    - 文件里写了的项按文件走，没写的项使用默认值；可用的键、默认值与写法见 config/README.md
    - YAML 只实现够用的子集，不依赖任何扩展；装了 PHP 的 yaml 扩展时优先用它解析
    - 配置文件解析失败（语法错、权限不足）不阻断启用，只在启用提示里说明原因并沿用已有配置；
      写入失败（目录只读）不影响禁用
    - 建表在应用文件配置之后进行，文件里指定的独立数据库会立刻生效；
      该库连不上时启用照常失败，且不会覆盖数据库中已有的配置
    - 配置文件含数据库密码与接口令牌，写出时权限设为 0600，config/ 下附带 .htaccess 与
      空 index.html 阻止 Web 直接下载（Nginx 下需自行在站点配置中拒绝该目录），
      并已将 config/current.yaml 加入 .gitignore

v3.1.1
2026年08月24日
    - 建表时增加两个覆盖索引 (time, ip) 与 (time, ip, ua)，概览统计不再需要回表
      （PostgreSQL 实测走 Index Only Scan，Heap Fetches 归零）；从 3.0.x 升级的老表
      需手动执行对应的 CREATE INDEX，语句见 sql/ 下的建表脚本
    - 概览页改为分段加载：今日 / 昨日 / 总计 / 来源 / 饼图 / 当月各自请求并逐步填充，
      修复数据量大时 overview.json 整体超时（504）导致缓存永远建不起来的死循环
    - 历史日期的统计结果长期缓存（过去的数据不会再变），当月图表按天推进，一次跑不完可续算

v3.1.0
2026年08月22日
    - 支持 PostgreSQL 数据库
    - 支持将统计数据存放到独立的 MySQL / PostgreSQL 数据库，与 Typecho 主库分离
    - 独立数据库配置在保存前会先测试连接，启用时自动迁移主库中的历史数据
    - 大表迁移提供两种方式：统计控制台的进度条（浏览器分片调用，无需 SSH）和命令行脚本 tools/migrate.php
    - 迁移采用 keyset 分页 + 批量写入，支持断点续传，未完成时如实显示剩余行数
    - 修复文章筛选下拉框的 SQL 分组字段缺失问题
    - 修复按 IPv6 地址筛选日志无效的问题
    - 新增写入队列：配置了 Redis 时自动启用，访问日志先入队再批量落库，显著降低突发流量下的数据库连接数
    - 新增命令行脚本 tools/flush-queue.php，可挂 cron 兜底刷库
    - 统计写入失败不再影响博客前台访问
    - 修复 PostgreSQL 报错时接口返回空响应的问题：Typecho 适配器会把含字母的 SQLSTATE 当成 int，
      抛出的 TypeError 逃出了只捕获 Exception 的 catch，导致控制台只能看到「无法获取」
    - 控制台加载失败时改为显示真实错误信息，并对丢失原始信息的数据库错误主动探测给出结论
    - 不再依赖 PHP calendar 扩展（cal_days_in_month 换成 date('t')）
    - 修复插件设置页报 Undefined array key dbMigrateDone：迁移完成标记不再混进插件配置，
      改存独立的 options 行；早期版本残留的键会在打开设置页时自动清理并平滑继承
    - 最低 PHP 版本要求提升至 8.2，启用时会检查
    - 数据库类型、驱动、迁移状态改用枚举，替换原先的字符串比较

v3.0.1
2026年03月08日
    - 更新 Highcharts 组件到 v11.3.0
    - 更新接口数据结构
    - 增加转换函数，将接口返回定位信息转换简体中文
    - 修复日志删除事件绑定错误的问题

v3.0.0
2026年03月05日
    - 使用新版开发规范重构，仅支持 Typecho 1.2+
    - 重构代码结构，优化性能和稳定性
    - 优化 IP 解析功能，增加 Lite 版本和 Core 版本支持

v2.4.0
2026月02月01日
    - 适配 Typecho 1.3.0 beta
    - 修复按**文章**筛选功能异常问题

v2.3.8
2021年04月08日
    - 增加更多UA标识

v2.3.7
2021年03月12日
    - 修正排版符

v2.3.6
2021年03月03日
    - 完善文字描述

v2.3.5
2021年01月26日
    - 增加微信 UA 识别

v2.3.4
2020年07月14日
    - 修复 Chromium 内核表格样式问题
    - 修正文字提示

v2.3.3
2020年07月10日
    - 增加新UA判断
    - 更新文档说明
    - 开发分支合并主分支

v2.3.2
2020年01月17日
    - 修正数据库初始化问题
    - 更新文档内容

v2.3.1
2020年01月10日
    - 修复控制台无法显示 IPv6 地址的问题

v2.3.0
2020年01月09日
    - 支持 IPv6 （测试版）

v2.2.3
2019年12月20日
    - 优化 SQL 语句
    - 移除 360极速浏览器 支持
    - 更新 Highcharts 组件到 v8.0
    - 移除老旧的IP分析用本地数据库，使用线上高精度接口替代

v2.2.2
2019年12月01日
    - 添加 Firefox Focus 浏览器 支持
    - 小问题修复
    - 添加 三星手机浏览器 支持
    - 添加 夸克浏览器 国际版 支持（UC Turbo）
    - 添加 2345浏览器 支持
    - 添加 微软 Edge Android/iOS 浏览器 支持
    - 修复部分浏览器识别错误问题
